# inventory-reorder-alert
This Python project reads inventory data from a CSV file, compares each item's quantity with its reorder threshold, identifies low-stock items, and generates a restock report.
